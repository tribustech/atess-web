# atess-web-next env example
# Copy to .env.local and fill in values

# Configurator email (Postmark)
POSTMARK_API_KEY=
POSTMARK_FROM=configurator@atess.ro
POSTMARK_STREAM=outbound
TEAM_EMAIL=teo@atess.ro

# Sveltia CMS (/admin): no server env needed — editors sign in with a
# fine-grained GitHub personal access token scoped to this repo.
