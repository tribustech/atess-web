# atess-web-next env example
# Copy to .env.local and fill in values

# Configurator email (Postmark)
POSTMARK_API_KEY=
POSTMARK_FROM=configurator@atess.ro
POSTMARK_STREAM=outbound
TEAM_EMAIL=teo@atess.ro

# Sveltia CMS — GitHub OAuth relay (/admin)
# Create a GitHub OAuth App with callback URL: https://atess.ro/api/auth/callback
CMS_GITHUB_CLIENT_ID=
CMS_GITHUB_CLIENT_SECRET=
# Optional: override OAuth base URL (defaults to NEXT_PUBLIC_SITE_URL, then request host)
# CMS_OAUTH_BASE_URL=https://atess.ro
